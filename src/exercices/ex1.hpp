#include <iostream>
#include <array>
#include <vector>
#include <numeric>
#include <iterator>
#include "../inputs.hpp"

#pragma once

/**
 * Retourne toutes les valeurs impaires de 1 à n - 1.
 *
 * @param n Le nombre de fois à boucler.
 *
 * @return 0
 */
std::vector<int> get_even_numbers(const int &n);

void ex1();
